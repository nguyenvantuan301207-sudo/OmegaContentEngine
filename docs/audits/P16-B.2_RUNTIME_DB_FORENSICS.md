# P16-B runtime database fixture provenance

This note records read-only evidence for a later controlled cleanup or quarantine. It does not
authorize deletion or modification of runtime rows.

- Runtime target: PostgreSQL service `omega-postgres`, database `omega`, schema `public`.
- Test insertion window: 2026-09-12 19:47:38Z through 20:08:02Z.
- `publish_intents` inserted in that window: 168.
- `publisher_scheduler_handoff_outbox` inserted in that window: 12.
- All 168 intents joined to mission titles that exactly match integration-test fixture literals in
  the repository; zero rows in the window were unmatched.
- Attribution by fixture family: 33 P16-B safety-test intents, 42 publisher-service-test intents,
  and 93 intents from other known integration fixtures.
- The 12 handoffs were 9 `DELIVERED` and 3 `DEAD_LETTER`; all were linked to known publisher test
  fixtures.
- The earlier count of 50 “undelivered” handoffs referred to 50 pre-existing `DEAD_LETTER` rows
  with null `delivered_at`, not rows transitioned by P16-B.

Do not identify cleanup candidates by timestamp alone. A future approved cleanup must revalidate
fixture provenance using relationships and repository-known test markers, produce an exact count,
and take a recoverable backup before any write.
